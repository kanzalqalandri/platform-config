# Cluster add-on definition (upstream Helm chart). Read by the cluster-addons appset.
chartRepo: https://charts.jetstack.io
chart: cert-manager
chartVersion: v1.20.0
namespace: cert-manager
syncWave: "-5"          # foundational controllers install early (negative waves first)
